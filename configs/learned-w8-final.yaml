experiment: learned_w8_final
choices:
  system: learned
  dataset: imagenette160
  split: train
  # W8 loader replaces this direct placeholder with the exact DEC-11 cell ratio
  # before hashing the resolved RunConfig; it never inherits a W7 ratio.
  bw_ratio: r_1_6
  channel: awgn
  train_snr_db: train_snr_db_fixed
  lambda: lambda_core
  classifier_variant: clean
  architecture: djscc_residual_v1
  execution_profile_id: confessor_pascal_cu126
  artifact_role: W8_FINAL_MULTI_SEED_RUN
  physical_batch_size: 32
  accumulation_factor: 1
  effective_batch_size: 32
  validation_batch_size: 32
sweep_axes:
  train_seed: train_seeds
  channel_seed: channel_seeds
